NODE_ENV=development
HOST=127.0.0.1
PORT=8787

# Optional persistence modes:
# 1) Postgres snapshot persistence (preferred outside tests)
# DATABASE_URL=postgres://user:pass@localhost:5432/insightfoundry
#
# 2) File persistence (default when DATABASE_URL is not set)
# STATE_SNAPSHOT_PATH=.runtime/state-snapshot.json

# Optional CORS for split frontend/backend deployments.
# Example: ALLOW_ORIGIN=http://localhost:5173
ALLOW_ORIGIN=

# Optional app metadata shown on /healthz
APP_VERSION=0.1.0
